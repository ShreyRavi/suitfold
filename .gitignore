node_modules/
dist/
*.log

# Data never belongs in the repo. It lives in ~/.suitfold/ so that a stray
# `git clean -fd` cannot destroy the season ledger.
*.sqlite
*.sqlite-wal
*.sqlite-shm
recaps/
.gstack/
